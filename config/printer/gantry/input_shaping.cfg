[input_shaper]
## MEASURED 2026-07-26 via SHAPER_CALIBRATE, one axis at a time.
##
## The previous values (mzv x=47.6, y=33) are NOT comparable. They were taken with
## adxl345 axes_map at the default x,y,z -- wrong for this board, gravity read on
## sensor X instead of Z -- and at probe_points 100,100 rather than bed centre.
## Both were inherited from BTT's sample config for a smaller printer.
##
##   X sweep:  zv 47.8Hz/6.1%   mzv 51.2Hz/4.7%   ei 46.8Hz/0.0%
##             2hump_ei 78.6Hz/0.0% (smoothing 0.099)   3hump_ei 98.6Hz/0.0%
##   Y sweep:  zv 36.8Hz/6.3%   mzv 35.8Hz/0.0% (smoothing 0.159)
##             ei 44.0Hz/0.0%   2hump_ei 56.6Hz/0.0%   3hump_ei 71.0Hz/0.0%
##
## Taking Klipper's recommendation per axis. Runner-up on X is mzv 51.2Hz, which has
## slightly LESS smoothing (0.084 vs 0.099) at the cost of 4.7% residual vibration --
## worth trying if corners look soft but no ringing is visible.
#   A frequency (in Hz) of the input shaper for X axis. This is
#   usually a resonance frequency of X axis that the input shaper
#   should suppress. For more complex shapers, like 2- and 3-hump EI
#   input shapers, this parameter can be set from different
#   considerations. The default value is 0, which disables input
#   shaping for X axis.
shaper_freq_x: 78.6

#   A frequency (in Hz) of the input shaper for Y axis. This is
#   usually a resonance frequency of Y axis that the input shaper
#   should suppress. For more complex shapers, like 2- and 3-hump EI
#   input shapers, this parameter can be set from different
#   considerations. The default value is 0, which disables input
#   shaping for Y axis.
shaper_freq_y: 35.8

#   If shaper_type is not set, these two parameters can be used to
#   configure different input shapers for X and Y axes. The same
#   values are supported as for shaper_type parameter.
shaper_type_x: 2hump_ei
shaper_type_y: mzv

#   Damping ratios of vibrations of X and Y axes used by input shapers
#   to improve vibration suppression. Default value is 0.1 which is a
#   good all-round value for most printers. In most circumstances this
#   parameter requires no tuning and should not be changed.
damping_ratio_x: 0.1
damping_ratio_y: 0.1

[resonance_tester]
#   Sole owner of [resonance_tester].  gantry/rezonance.cfg and
#   stealburner/resonance.cfg used to redefine these and silently won --
#   which is how probe_points ended up at 100,100 instead of bed centre.
accel_chip: adxl345

#   A list of X, Y, Z coordinates of points (one point per line) to test
#   resonances at. At least one point is required. Make sure that all
#   points with some safety margin in XY plane (~a few centimeters)
#   are reachable by the toolhead.
#   175,175 is the centre of a 350mm bed -- measure resonances there.
probe_points: 175,175,20

#   Maximum input shaper smoothing to allow for each axis during shaper
#   auto-calibration (with 'SHAPER_CALIBRATE' command). By default no
#   maximum smoothing is specified. Refer to Measuring_Resonances guide
#   for more details on using this feature.
#max_smoothing:

#   Minimum frequency to test for resonances. The default is 5 Hz.
min_freq: 5

#   Maximum frequency to test for resonances. The default is 133.33 Hz.
max_freq: 133.33

#   This parameter is used to determine which acceleration to use to
#   test a specific frequency: accel = accel_per_hz * freq. Higher the
#   value, the higher is the energy of the oscillations. Can be set to
#   a lower than the default value if the resonances get too strong on
#   the printer. However, lower values make measurements of
#   high-frequency resonances less precise. The default value is 75
#   (mm/sec).
## 75 -> 50. accel_per_hz sets the test acceleration as accel = accel_per_hz * freq,
## so at the 133Hz top of the sweep 75 demands 9975 mm/s^2 -- nearly double this
## printer's max_accel of 5700. Combined with the ADXL streaming 3200 samples/s from
## the EBB across the same 1Mbit CAN bus the MP8 uses for step timing, that produced:
##     Unable to obtain 'spi_transfer_response'
##     MCU 'MP8' shutdown: Timer too close
## Host load was 0.42 and the bus showed zero errors, so this was bandwidth and
## timing headroom, not an overloaded host (which is what Klipper's message guesses).
## 50 keeps the top-of-sweep accel at ~6650 mm/s^2, much closer to the machine's limit.
accel_per_hz: 50

#   Determines the speed of the test. When testing all frequencies in
#   range [min_freq, max_freq], each second the frequency increases by
#   hz_per_sec. Small values make the test slow, and the large values
#   will decrease the precision of the test. The default value is 1.0
#   (Hz/sec == sec^-2).
hz_per_sec: 1